<Window x:Class="SchuhLadenApp.ShowAllArticles"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchuhLadenApp"
        mc:Ignorable="d"
        Title="ShowAllArticles" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="gridArticleView">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Article ID" Binding="{Binding ArtikelId}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding Lieferant}"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Preis}"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Menge}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnEdit" Content="Button" HorizontalAlignment="Left" Margin="750,406,0,0" VerticalAlignment="Top"/>
        <Button x:Name="btnBack" Content="Button" HorizontalAlignment="Left" Margin="10,406,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
